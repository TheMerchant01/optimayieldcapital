import React from "react";
// import EditDepositHistory from "../../../components/admin/EditDepositHistory/EditDepositHistory";

export default function page({ params }) {
  const { id, amount, dateAdded, email } = params;

  // return <EditDepositHistory id={id} amount={amount} dateAdded={dateAdded} email={email}/>;
  return <div></div>;
}